--- kernel/exit.c
+++ kernel/exit.c
@@ -590,6 +590,7 @@ static void exit_mm(void)
      BUG_ON(mm != current->active_mm);
      /* more a memory barrier than a real lock */
      task_lock(current);
+     current->user_dumpable = (get_dumpable(mm) == SUID_DUMP_USER);
      current->mm = NULL;
      up_read(&mm->mmap_sem);
      enter_lazy_tlb(mm, current);
